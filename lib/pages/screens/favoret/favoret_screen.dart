import 'package:flutter/material.dart';

class FavoretScreen extends StatelessWidget {
  const FavoretScreen({super.key});
  static const String route = 'FavoretScreen';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favoret'),
    );
  }
}
